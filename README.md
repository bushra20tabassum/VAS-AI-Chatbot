# VAS-AI-Chatbot
VAS AI Chatbot is a web-based chatbot that answers FAQs about Value Added Services (VAS) using NLP. It preprocesses user queries, matches them with the most relevant FAQs using TF-IDF and cosine similarity, and displays responses in a modern, interactive chat interface with confidence scores.
